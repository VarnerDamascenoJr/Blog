{{#each erros}}
<div class="alert alert-danger"> 
  {{texto}}
</div>
{{else}}
<h4 class="mt-4">Nenhuma postagem</h4>
{{/each}}
<h3 class="">Nova Categoria</h3>
<div class="card">
  <div class="card-body">
<form class="" action="/admin/categorias/nova" method="POST">
  <label for="nome">Nome</label>
  <input type="text" name="nome" id="nome" placeholder="Nome da categoria"value="" class="form-contrl"><br>
  <label for="slug">Slug </label>
  <input type="text" name="slug" id="slug" placeholder="Slug da categoria" value=""  class="form-contrl">
  <br>
  <button type="submit" name="button" class="btn btn-success">Criar Categoria</button>
  
  <form action="admin/categorias/deletar" >Deletar</form>
      <input type="hidden" name="id" value="{{_id}}">
     <button type="submit" class="btn btn-danger mt-3">Deletar Categoria</button>

</form>
</div>
</div>
