{{#each erros}}
<div class="alert alert-danger">
  {{texto}}
</div>
{{else}}
{{/each}}
<h3>Editar Categoria</h3>
<div class="card">
  <div class="card-body">
<form class="" action="/admin/categorias/edit" method="POST">
  <input type="hidden" name="id" value="{{categoria._id}}">
  <label for="nome">Nome</label>
  <input type="text" name="nome" id="nome" placeholder="Nome da categoria"value="{{categoria.nome}}" class="form-contrl" ><br>
  <label for="slug">Slug </label>
  <input type="text" name="slug" id="slug" placeholder="Slug da categoria" value="{{categoria.slug}}"  class="form-contrl">
  <br>
  <button type="submit" name="button" class="btn btn-success">Editar Categoria</button>

</form>
</div>
</div>
